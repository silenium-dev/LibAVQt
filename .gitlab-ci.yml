build:
  stage: build
  image: wiktorguz/qt6
  script:
    - mkdir build
    - cd build
    - cmake -DCMAKE_BUILD_TYPE=Debug ..
    - cmake --build . -j4
  artifacts:
    paths:
      - build/AVQt/libAVQt.so
      - AVQt/
    expire_in: 1 week
